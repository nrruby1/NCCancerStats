import { MainTabsDirective } from './main-tabs.directive';

describe('MainTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new MainTabsDirective();
    expect(directive).toBeTruthy();
  });
});
